workflows:
  android-workflow:
    name: Android Workflow
    environment:
      java: 17
    scripts:
      - name: Build Android Debug APK
        script: |
          ./gradlew assembleDebug
    artifacts:
      - app/build/outputs/**/*.apk
